"""Steamworks init / achievements — stub for a later epic.

When Steamworks SDK is linked, call init() at shell startup and shutdown() on exit.
"""


def init() -> bool:
    """Return True when Steam API is available. v0.1 always False."""
    return False


def shutdown() -> None:
    return None
